# Review findings

`F-XXX-<aspect>-pass-N.md`, written by `/microscope` and tracked in git.

One file per pass, and the pass number matters: a rising count with changing
findings is progress, the same finding surviving three passes is not.

Every report carries a `## Verified clean` section. Without it, a thorough pass
and a pass that looked at nothing produce identical documents.
