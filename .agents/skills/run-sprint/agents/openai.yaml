interface:
  display_name: "Run Sprint"
  short_description: "Run an entire sprint autonomously, design through consolidated veri..."
  default_prompt: "Use $run-sprint to follow the repository workflow."
policy:
  allow_implicit_invocation: false
