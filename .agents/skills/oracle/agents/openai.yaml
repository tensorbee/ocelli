interface:
  display_name: "Oracle"
  short_description: "Run the differential harness against cornerstone3D over the corpus,..."
  default_prompt: "Use $oracle to follow the repository workflow."
policy:
  allow_implicit_invocation: false
