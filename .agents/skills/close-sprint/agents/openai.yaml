interface:
  display_name: "Close Sprint"
  short_description: "Close a sprint. Validates readiness, merges to main, creates the sN..."
  default_prompt: "Use $close-sprint to follow the repository workflow."
policy:
  allow_implicit_invocation: false
