interface:
  display_name: "Sync Status"
  short_description: "Audit consistency across the backlog, sprint plan, tracker, as-buil..."
  default_prompt: "Use $sync-status to follow the repository workflow."
policy:
  allow_implicit_invocation: false
