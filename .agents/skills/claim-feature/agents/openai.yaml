interface:
  display_name: "Claim Feature"
  short_description: "Reserve one F-ID for one agent in one worktree, before parallel imp..."
  default_prompt: "Use $claim-feature to follow the repository workflow."
policy:
  allow_implicit_invocation: false
