interface:
  display_name: "Implement Feature"
  short_description: "Implement one F-ID from its approved design plan, with the focused..."
  default_prompt: "Use $implement-feature to follow the repository workflow."
policy:
  allow_implicit_invocation: false
