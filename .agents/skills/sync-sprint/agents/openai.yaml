interface:
  display_name: "Sync Sprint"
  short_description: "Open a sprint. Creates the branch, rewrites CURRENT_SPRINT.md from..."
  default_prompt: "Use $sync-sprint to follow the repository workflow."
policy:
  allow_implicit_invocation: false
