interface:
  display_name: "Design"
  short_description: "Write the design plan for one F-ID against the normative HLD, and g..."
  default_prompt: "Use $design to follow the repository workflow."
policy:
  allow_implicit_invocation: false
