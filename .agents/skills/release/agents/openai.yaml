interface:
  display_name: "Release"
  short_description: "Publish a prepared and reviewed version to crates.io and npm. The o..."
  default_prompt: "Use $release to follow the repository workflow."
policy:
  allow_implicit_invocation: false
