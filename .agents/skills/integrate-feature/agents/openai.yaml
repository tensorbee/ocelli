interface:
  display_name: "Integrate Feature"
  short_description: "Merge one prepared worker branch into the canonical sprint worktree..."
  default_prompt: "Use $integrate-feature to follow the repository workflow."
policy:
  allow_implicit_invocation: false
