interface:
  display_name: "Parity"
  short_description: "Report progress against the cornerstone3D parity surface, from the..."
  default_prompt: "Use $parity to follow the repository workflow."
policy:
  allow_implicit_invocation: false
